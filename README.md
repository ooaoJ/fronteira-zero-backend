# Como rodar o projeto:

## Backend:
  Rode o backend com o comando: npm run start:dev

## Frontend:
  Configure o .env para achar a url da api (coloque a url que aponta para onde ela está hospedada).
  E depois rode: npm run dev